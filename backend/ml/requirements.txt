# Deps for running ml/train_predict.py standalone. The server now trains too
# (round_predictions.py), so these are also pinned in the main
# requirements.txt — keep the two in lockstep.
# model.pkl is only guaranteed loadable with the same scikit-learn minor version.
numpy==2.2.*
scikit-learn==1.7.*
